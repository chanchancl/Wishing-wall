
1.需要3个主模板
    1.index模板
        遍历db，将已有的记录显示出来（每页有最大显示数目，或者做成可以向下添加的）
    2.add模板
        可以提交记录
    3.delete模板
        可以删除记录
        
2.记录的提交与删除
    每条记录都有一个独一无二的ID，用于识别
    每条记录有一个 字符数据段
    每条记录有一个对应的 密码（用于上传者删除）

3.css 和 js 到后面再说
    
4.先这样


想用模态框来 add/del 结果出了点问题。。

database 和 module 有点。。。